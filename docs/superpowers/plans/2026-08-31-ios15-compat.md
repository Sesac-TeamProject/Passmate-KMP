# iOS 15 호환 (배포 타깃 15.0 복귀) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** iOS 앱의 최소 배포 타깃을 15.0으로 내리고, iOS 16+ 전용 API(`NavigationStack`·`Layout`·`presentationDetents`)를 iOS 15 호환 구현으로 교체해 팀 실기기(iOS 15)에서 동작하게 한다.

**Architecture:** 탭 셸은 `NavigationView`(stack) 하나가 `TabView`를 감싸는 **단일 `[Route]` 스택**으로 바꾸고, 배열 기반 push는 재귀 뷰 `RouteStackLevel`(레벨당 숨은 `NavigationLink` 1개)이 담당한다. 칩 줄바꿈은 `Layout` 프로토콜 대신 데이터 기반 `FlowLayout`(alignmentGuide 기법) 공통 1벌로, 시트 반높이는 `passmateDetents()` modifier(16+ 네이티브 / 15 UIKit `UISheetPresentationController` 브리지)로 통일한다. 화면 파일은 호출부 한 줄 치환 외 무변경.

**Tech Stack:** SwiftUI(iOS 15.0 배포 타깃) · UIKit `UISheetPresentationController`(iOS 15.0+) · Xcode 26.3 · Kotlin/Native `Shared` 프레임워크(무변경)

**Spec:** `docs/superpowers/specs/2026-08-31-ios15-compat-design.md`

## Global Constraints

- 코드 규칙 `docs/Passmate_코드_패턴_규칙.md` 전부 적용. 특히 §11-1(컨테이너/콘텐츠 뷰 분리), §11-2(`PassmateColors` 토큰만, hex 금지), §16(프로퍼티 한 줄씩+개행, private 메서드 상단·public 하단·`init` 최하단, `if-else` 우선, 3단계 이상 중첩만 조기 반환 허용)
- **최소 배포 타깃 iOS 15.0.** iOS 16+ 전용 API(`NavigationStack`·`navigationDestination`·`toolbar(_:for:)`·`presentationDetents`·`Layout`·`ProposedViewSize` 등)는 화면에서 직접 쓰지 않는다. 16+ 분기가 필요하면 `component/` 공통 컴포넌트 안에서 `if #available(iOS 16.0, *)`로만 쓴다
- **`shared`·`composeApp`(Kotlin)은 변경하지 않는다.** 변경 대상은 `iosApp/`과 `docs/`만
- 화면 파일(`ui/**`)은 이 계획서에 적힌 줄만 바꾼다. `destinationView(for:path:)` 본문(`ContentView.swift:117~211`)은 무변경
- 새 파일은 LF 개행, 파일명 = 타입명 1:1, 파일 머리에 `import SwiftUI`(UIKit 필요 시 `import UIKit` 추가)
- pbxproj 신규 파일 idx는 **155~158 고정**(155 `RouteStackLevel`, 156 `FlowLayout`, 157 `WeakTopicsRow`, 158 `SheetDetents`), ID 형식 `A1010001{idx}AABBCCDDEEFF0{idx}`(BuildFile)·`A1011001{idx}AABBCCDDEEFF0{idx}`(FileReference). 그룹 ID(`A10120xx`)는 새로 만들거나 바꾸지 않는다. 편집 후 `plutil -lint` OK + 중복 ID 0개
- `xcodebuild`는 샌드박스 해제 + 절대경로로 실행한다. Xcode 빌드 스크립트가 `gradlew`에 +x를 붙이므로 **매 빌드 후 `git checkout -- gradlew`**, 커밋 전 `git status --short`에 `gradlew`가 없어야 한다
- 빌드 명령 2종(각 스텝에 전문으로 적혀 있음 — 반드시 `cd /Users/pocari/workspace/projects/pass-mate/Passmate-KMP` 후 실행):
  - **iOS 15 검사 빌드**(배포 타깃을 명령줄로 덮어써 오류 전체 수집):
    ```bash
    xcodebuild -project /Users/pocari/workspace/projects/pass-mate/Passmate-KMP/iosApp/iosApp.xcodeproj -scheme iosApp -configuration Debug -destination "generic/platform=iOS Simulator" -derivedDataPath /Users/pocari/workspace/projects/pass-mate/Passmate-KMP/iosApp/build/DerivedData CODE_SIGNING_ALLOWED=NO IPHONEOS_DEPLOYMENT_TARGET=15.0 'OTHER_SWIFT_FLAGS=$(inherited) -continue-building-after-errors' build
    ```
  - **일반 빌드**(pbxproj 설정값 그대로):
    ```bash
    xcodebuild -project /Users/pocari/workspace/projects/pass-mate/Passmate-KMP/iosApp/iosApp.xcodeproj -scheme iosApp -configuration Debug -destination "generic/platform=iOS Simulator" -derivedDataPath /Users/pocari/workspace/projects/pass-mate/Passmate-KMP/iosApp/build/DerivedData CODE_SIGNING_ALLOWED=NO build
    ```
- 스크래치 디렉터리 `SCRATCH=/private/tmp/claude-501/-Users-pocari-workspace-projects-pass-mate-Passmate-KMP/582e7a59-aee1-47e1-91d2-6a86fe25d44e/scratchpad` — 로그·스크린샷은 여기에
- 브랜치 `fix/ios15-deployment-target`(← `feature/home` 4888f0f). **push·PR 생성은 사용자 승인 후에만.** `feature/result` 병합 금지
- 커밋 메시지 끝에 빈 줄 뒤 `Claude-Session: https://claude.ai/code/session_01KmjdULniHZh2JYamx3rkjj` 한 줄

## 파일 구조

**신규 (`iosApp/iosApp/`)**
- `navigation/RouteStackLevel.swift` — `[Route]`의 한 레벨을 그리고 다음 레벨로 가는 숨은 `NavigationLink` 1개를 가진 재귀 뷰 (`NavigationStack(path:)` 대체)
- `component/FlowLayout.swift` — 데이터 기반 줄바꿈 레이아웃(iOS 14+ alignmentGuide 기법) + 높이 `PreferenceKey`
- `component/WeakTopicsRow.swift` — "보완할 주제" 라벨+칩 행(ResultView·JoinedRoomsView 공용, 승격)
- `component/SheetDetents.swift` — `PassmateSheetDetent` enum + `View.passmateDetents(_:)` + iOS 15 UIKit 브리지

**수정 (`iosApp/iosApp/`)**
- `ContentView.swift` — 상태 `path: [Route]` 하나, `NavigationView { TabView … .background(NavigationLink → RouteStackLevel) }`, `tabStack()`·`currentPath` 삭제
- `ui/result/RatingSectionView.swift` — `FlowLayout` 호출부 데이터 기반으로, private `FlowLayout: Layout` 삭제
- `ui/result/ResultView.swift` — private `WeakTopicsRow`·`FlowLayout` 삭제, `.passmateDetents([.large])`
- `ui/mypage/JoinedRoomsView.swift` — private `WeakTopicsRow`·`FlowLayout` 삭제
- `ui/home/RoomListView.swift` · `ui/hostroom/HostedRoomsView.swift` · `ui/payment/EarningsView.swift` · `ui/mypage/MyInfoView.swift` — `.presentationDetents` → `.passmateDetents` 한 줄씩
- `iosApp.xcodeproj/project.pbxproj` — `IPHONEOS_DEPLOYMENT_TARGET = 15.0` 2곳, 신규 4파일 등록(16줄)

**문서**
- `docs/Passmate_코드_패턴_규칙.md` §2-1(iOS 문구 교체 + 배포 타깃 규칙 추가) · §13(금지 1줄)
- `docs/superpowers/specs/2026-08-30-home-shell-tabs-design.md` §1-4 iOS 행 · §5 첫 줄 정정
- `docs/Passmate_Mac_검증_체크리스트.md` §10 신설

---

### Task 1: 시트 반높이 — `passmateDetents` 브리지 + 호출부 5곳

**Files:**
- Create: `iosApp/iosApp/component/SheetDetents.swift`
- Modify: `iosApp/iosApp/ui/home/RoomListView.swift:63`, `iosApp/iosApp/ui/result/ResultView.swift:56`, `iosApp/iosApp/ui/hostroom/HostedRoomsView.swift:63`, `iosApp/iosApp/ui/payment/EarningsView.swift:49`, `iosApp/iosApp/ui/mypage/MyInfoView.swift:86`
- Modify: `iosApp/iosApp.xcodeproj/project.pbxproj` (idx 158 등록 4줄)

**Interfaces:**
- Produces: `enum PassmateSheetDetent { case medium, large }`, `extension View { func passmateDetents(_ detents: [PassmateSheetDetent]) -> some View }`

- [ ] **Step 1: 기준(레드) 확보 — iOS 15 검사 빌드로 오류 20건 기록**

```bash
cd /Users/pocari/workspace/projects/pass-mate/Passmate-KMP
SCRATCH=/private/tmp/claude-501/-Users-pocari-workspace-projects-pass-mate-Passmate-KMP/582e7a59-aee1-47e1-91d2-6a86fe25d44e/scratchpad
xcodebuild -project /Users/pocari/workspace/projects/pass-mate/Passmate-KMP/iosApp/iosApp.xcodeproj -scheme iosApp -configuration Debug -destination "generic/platform=iOS Simulator" -derivedDataPath /Users/pocari/workspace/projects/pass-mate/Passmate-KMP/iosApp/build/DerivedData CODE_SIGNING_ALLOWED=NO IPHONEOS_DEPLOYMENT_TARGET=15.0 'OTHER_SWIFT_FLAGS=$(inherited) -continue-building-after-errors' build > "$SCRATCH/ios15-baseline.log" 2>&1
grep -E "error:" "$SCRATCH/ios15-baseline.log" | sed 's#.*/iosApp/iosApp/##' | sort -u
grep -E "error:" "$SCRATCH/ios15-baseline.log" | sort -u | wc -l
git checkout -- gradlew
```
Expected: 20줄. 그중 `presentationDetents' is only available in iOS 16.0 or newer` 5줄(RoomListView:63 · ResultView:56 · HostedRoomsView:63 · EarningsView:49 · MyInfoView:86).

- [ ] **Step 2: `SheetDetents.swift` 작성**

```swift
import SwiftUI
import UIKit

// 시트 높이(반높이·전체) 공통 modifier — iOS 16+는 presentationDetents, iOS 15는 UIKit UISheetPresentationController로 동일 결과
// (스펙 2026-08-31 §4, 규칙 §2-1: iOS 16+ API는 공통 컴포넌트 안 #available 분기로만)
enum PassmateSheetDetent {
    case medium
    case large
}

extension View {
    func passmateDetents(_ detents: [PassmateSheetDetent]) -> some View {
        modifier(PassmateSheetDetentsModifier(detents: detents))
    }
}

private struct PassmateSheetDetentsModifier: ViewModifier {
    let detents: [PassmateSheetDetent]

    @available(iOS 16.0, *)
    private var nativeDetents: Set<PresentationDetent> {
        Set(detents.map { detent -> PresentationDetent in
            switch detent {
            case .medium: return .medium
            case .large: return .large
            }
        })
    }

    func body(content: Content) -> some View {
        if #available(iOS 16.0, *) {
            content.presentationDetents(nativeDetents)
        } else {
            content.background(SheetDetentsBridge(detents: detents))
        }
    }
}

// iOS 15: 시트 콘텐츠 뒤에 빈 컨트롤러를 심고, 조상 UISheetPresentationController의 detents를 설정한다.
// 선택 detent를 따로 지정하지 않으면 배열의 첫 항목으로 열린다([.medium, .large] → 반높이 시작)
private struct SheetDetentsBridge: UIViewControllerRepresentable {
    let detents: [PassmateSheetDetent]

    func makeUIViewController(context: Context) -> SheetDetentsController {
        SheetDetentsController(detents: detents.map(\.uiKitDetent))
    }

    func updateUIViewController(_ uiViewController: SheetDetentsController, context: Context) {
        uiViewController.detents = detents.map(\.uiKitDetent)
        uiViewController.applyDetents()
    }
}

private final class SheetDetentsController: UIViewController {
    var detents: [UISheetPresentationController.Detent]

    func applyDetents() {
        sheetPresentationController?.detents = detents
    }

    override func viewWillAppear(_ animated: Bool) {
        super.viewWillAppear(animated)
        applyDetents()
    }

    init(detents: [UISheetPresentationController.Detent]) {
        self.detents = detents
        super.init(nibName: nil, bundle: nil)
    }

    required init?(coder: NSCoder) {
        fatalError("SheetDetentsController는 코드로만 생성한다")
    }
}

private extension PassmateSheetDetent {
    var uiKitDetent: UISheetPresentationController.Detent {
        switch self {
        case .medium: return .medium()
        case .large: return .large()
        }
    }
}
```

- [ ] **Step 3: 호출부 5곳 치환**

각 파일의 해당 줄에서 `.presentationDetents(` → `.passmateDetents(`로 바꾼다. 인자 배열은 그대로.

| 파일:줄 | 변경 전 | 변경 후 |
|---|---|---|
| `ui/home/RoomListView.swift:63` | `.presentationDetents([.medium, .large])` | `.passmateDetents([.medium, .large])` |
| `ui/result/ResultView.swift:56` | `.presentationDetents([.large])` | `.passmateDetents([.large])` |
| `ui/hostroom/HostedRoomsView.swift:63` | `.presentationDetents([.large])` | `.passmateDetents([.large])` |
| `ui/payment/EarningsView.swift:49` | `.presentationDetents([.medium, .large])` | `.passmateDetents([.medium, .large])` |
| `ui/mypage/MyInfoView.swift:86` | `.presentationDetents([.medium, .large])` | `.passmateDetents([.medium, .large])` |

확인: `grep -rn "presentationDetents" iosApp/iosApp --include='*.swift'` 결과가 `component/SheetDetents.swift` 1건만이어야 한다.

- [ ] **Step 4: pbxproj에 idx 158 등록 (4줄)**

`iosApp/iosApp.xcodeproj/project.pbxproj`에서 아래 앵커 줄 **바로 뒤**에 한 줄씩 추가한다(탭 인덴트 유지).

1. PBXBuildFile 섹션 — 앵커 `A1010001154AABBCCDDEEFF0154 /* SettingsView.swift in Sources */ = {isa = PBXBuildFile; fileRef = A1011001154AABBCCDDEEFF0154 /* SettingsView.swift */; };` 뒤:
```
		A1010001158AABBCCDDEEFF0158 /* SheetDetents.swift in Sources */ = {isa = PBXBuildFile; fileRef = A1011001158AABBCCDDEEFF0158 /* SheetDetents.swift */; };
```
2. PBXFileReference 섹션 — 앵커 `A1011001154AABBCCDDEEFF0154 /* SettingsView.swift */ = {isa = PBXFileReference; lastKnownFileType = sourcecode.swift; path = SettingsView.swift; sourceTree = "<group>"; };` 뒤:
```
		A1011001158AABBCCDDEEFF0158 /* SheetDetents.swift */ = {isa = PBXFileReference; lastKnownFileType = sourcecode.swift; path = SheetDetents.swift; sourceTree = "<group>"; };
```
3. `A1012021AABBCCDDEEFF0021 /* component */` 그룹 children — 앵커 `A1011066AABBCCDDEEFF0066 /* PassyMascot.swift */,` 뒤:
```
				A1011001158AABBCCDDEEFF0158 /* SheetDetents.swift */,
```
4. PBXSourcesBuildPhase files — 앵커 `A1010001154AABBCCDDEEFF0154 /* SettingsView.swift in Sources */,` 뒤:
```
				A1010001158AABBCCDDEEFF0158 /* SheetDetents.swift in Sources */,
```

검증:
```bash
plutil -lint iosApp/iosApp.xcodeproj/project.pbxproj
grep -E $'^\t\t[0-9A-F]+ /\\* .* \\*/ = \\{' iosApp/iosApp.xcodeproj/project.pbxproj | awk '{print $1}' | sort | uniq -d
grep -c "AABBCCDDEEFF0158" iosApp/iosApp.xcodeproj/project.pbxproj
```
Expected: `OK` / 빈 출력(중복 없음) / `4`

- [ ] **Step 5: 그린 확인 — iOS 15 검사 빌드에서 detents 오류 0, 일반 빌드 성공**

```bash
cd /Users/pocari/workspace/projects/pass-mate/Passmate-KMP
SCRATCH=/private/tmp/claude-501/-Users-pocari-workspace-projects-pass-mate-Passmate-KMP/582e7a59-aee1-47e1-91d2-6a86fe25d44e/scratchpad
xcodebuild -project /Users/pocari/workspace/projects/pass-mate/Passmate-KMP/iosApp/iosApp.xcodeproj -scheme iosApp -configuration Debug -destination "generic/platform=iOS Simulator" -derivedDataPath /Users/pocari/workspace/projects/pass-mate/Passmate-KMP/iosApp/build/DerivedData CODE_SIGNING_ALLOWED=NO IPHONEOS_DEPLOYMENT_TARGET=15.0 'OTHER_SWIFT_FLAGS=$(inherited) -continue-building-after-errors' build > "$SCRATCH/ios15-task1.log" 2>&1
grep -E "error:" "$SCRATCH/ios15-task1.log" | sed 's#.*/iosApp/iosApp/##' | sort -u
grep -cE "presentationDetents" "$SCRATCH/ios15-task1.log"
xcodebuild -project /Users/pocari/workspace/projects/pass-mate/Passmate-KMP/iosApp/iosApp.xcodeproj -scheme iosApp -configuration Debug -destination "generic/platform=iOS Simulator" -derivedDataPath /Users/pocari/workspace/projects/pass-mate/Passmate-KMP/iosApp/build/DerivedData CODE_SIGNING_ALLOWED=NO build 2>&1 | grep -E "\*\* BUILD|error:"
git checkout -- gradlew
```
Expected: 오류 15줄(ContentView 3 + Layout 12), `presentationDetents` 언급 0, 일반 빌드 `** BUILD SUCCEEDED **`.

- [ ] **Step 6: 커밋**

```bash
git status --short   # gradlew가 없어야 함
git add iosApp/iosApp/component/SheetDetents.swift iosApp/iosApp/ui/home/RoomListView.swift iosApp/iosApp/ui/result/ResultView.swift iosApp/iosApp/ui/hostroom/HostedRoomsView.swift iosApp/iosApp/ui/payment/EarningsView.swift iosApp/iosApp/ui/mypage/MyInfoView.swift iosApp/iosApp.xcodeproj/project.pbxproj
git commit -m "feat(ios): 시트 높이 공통 modifier passmateDetents — iOS 16+ presentationDetents / iOS 15 UISheetPresentationController 브리지, 호출부 5곳 치환 (iOS 15 호환 스펙 §4)

Claude-Session: https://claude.ai/code/session_01KmjdULniHZh2JYamx3rkjj"
```

---

### Task 2: `FlowLayout` iOS 15 구현 + `WeakTopicsRow` 승격 (private 중복 5벌 삭제)

**Files:**
- Create: `iosApp/iosApp/component/FlowLayout.swift`, `iosApp/iosApp/component/WeakTopicsRow.swift`
- Modify: `iosApp/iosApp/ui/result/RatingSectionView.swift:32-36`(호출부), `:134-179`(private FlowLayout 삭제)
- Modify: `iosApp/iosApp/ui/result/ResultView.swift:343-391`(private FlowLayout 삭제), `:216-241`(private WeakTopicsRow 삭제)
- Modify: `iosApp/iosApp/ui/mypage/JoinedRoomsView.swift:389-437`(private FlowLayout 삭제), `:250-275`(private WeakTopicsRow 삭제)
- Modify: `iosApp/iosApp.xcodeproj/project.pbxproj` (idx 156·157 등록 8줄)

**Interfaces:**
- Produces: `struct FlowLayout<Data: RandomAccessCollection, ID: Hashable, Content: View>: View` — `init(_ data: Data, id: KeyPath<Data.Element, ID>, spacing: CGFloat = 8, @ViewBuilder content: @escaping (Data.Element) -> Content)`
- Produces: `struct WeakTopicsRow: View` — `WeakTopicsRow(topics: [String])` (기존 private 버전과 호출 시그니처 동일)

- [ ] **Step 1: 레드 확인 — 기준 로그에서 Layout 오류 12건**

```bash
SCRATCH=/private/tmp/claude-501/-Users-pocari-workspace-projects-pass-mate-Passmate-KMP/582e7a59-aee1-47e1-91d2-6a86fe25d44e/scratchpad
grep -E "ProposedViewSize|callAsFunction" "$SCRATCH/ios15-baseline.log" | sed 's#.*/iosApp/iosApp/##' | sort -u | wc -l
```
Expected: `12` (ResultView 4 · RatingSectionView 4 · JoinedRoomsView 4)

- [ ] **Step 2: `FlowLayout.swift` 작성**

```swift
import SwiftUI

// 칩·태그를 가로로 흐르게 놓고 폭이 넘치면 줄바꿈하는 레이아웃 — iOS 15 호환(Layout 프로토콜 미사용).
// 자식을 열거하는 공개 API가 없어 데이터 기반으로 받는다: FlowLayout(items, id: \.self, spacing: 8) { item in chip(item) }
// 배치는 ZStack(.topLeading) + alignmentGuide 누적 오프셋, 전체 높이는 PreferenceKey로 되읽어 고정한다 (스펙 2026-08-31 §3-1)
struct FlowLayout<Data: RandomAccessCollection, ID: Hashable, Content: View>: View {
    private struct Entry: Identifiable {
        let id: ID

        let index: Int

        let element: Data.Element
    }

    private let entries: [Entry]

    private let spacing: CGFloat

    private let content: (Data.Element) -> Content

    @State private var totalHeight: CGFloat = 0

    private var heightReader: some View {
        GeometryReader { geometry in
            Color.clear.preference(key: FlowLayoutHeightKey.self, value: geometry.size.height)
        }
    }

    // alignmentGuide 클로저는 레이아웃 패스마다 자식 순서대로 호출된다(.leading 다음 .top).
    // 마지막 항목에서 누적값을 0으로 되돌려 다음 패스를 준비한다. 반환값은 음수 오프셋(leading/top 기준 이동량).
    private func rows(in geometry: GeometryProxy) -> some View {
        var x: CGFloat = 0
        var y: CGFloat = 0
        var rowHeight: CGFloat = 0
        let maxWidth = geometry.size.width
        let lastIndex = entries.count - 1

        return ZStack(alignment: .topLeading) {
            ForEach(entries) { entry in
                content(entry.element)
                    .alignmentGuide(.leading) { dimensions in
                        if x + dimensions.width > maxWidth, x > 0 {
                            x = 0
                            y += rowHeight + spacing
                            rowHeight = 0
                        }
                        let result = x

                        rowHeight = max(rowHeight, dimensions.height)
                        if entry.index == lastIndex {
                            x = 0
                        } else {
                            x += dimensions.width + spacing
                        }
                        return -result
                    }
                    .alignmentGuide(.top) { _ in
                        let result = y

                        if entry.index == lastIndex {
                            y = 0
                            rowHeight = 0
                        }
                        return -result
                    }
            }
        }
        .background(heightReader)
    }

    var body: some View {
        GeometryReader { geometry in
            rows(in: geometry)
        }
        .frame(height: totalHeight)
        .onPreferenceChange(FlowLayoutHeightKey.self) { totalHeight = $0 }
    }

    init(
        _ data: Data,
        id: KeyPath<Data.Element, ID>,
        spacing: CGFloat = 8,
        @ViewBuilder content: @escaping (Data.Element) -> Content
    ) {
        self.entries = data.enumerated().map { offset, element in
            Entry(id: element[keyPath: id], index: offset, element: element)
        }
        self.spacing = spacing
        self.content = content
    }
}

private struct FlowLayoutHeightKey: PreferenceKey {
    static var defaultValue: CGFloat = 0

    static func reduce(value: inout CGFloat, nextValue: () -> CGFloat) {
        value = nextValue()
    }
}
```

- [ ] **Step 3: `WeakTopicsRow.swift` 작성**

```swift
import SwiftUI

// "보완할 주제" 라벨 + 주제 칩 행 — ResultView(M-07)·JoinedRoomsView(M-08) 공용(두 화면의 private 복사본을 승격).
// 라벨이 칩들과 같은 흐름에 놓이므로 항목 enum으로 표현한다 (스펙 2026-08-31 §3-2)
struct WeakTopicsRow: View {
    private enum Item: Hashable {
        case label

        case topic(String)
    }

    let topics: [String]

    private var items: [Item] {
        [.label] + topics.map(Item.topic)
    }

    @ViewBuilder
    private func itemView(_ item: Item) -> some View {
        switch item {
        case .label:
            Text("보완할 주제")
                .font(.system(size: 14, weight: .medium))
                .kerning(-0.28)
                .foregroundColor(PassmateColors.textSecondary)
                .padding(.vertical, 6)
        case let .topic(topic):
            Text(topic)
                .font(.system(size: 14, weight: .medium))
                .kerning(-0.28)
                .foregroundColor(PassmateColors.weakTopicText)
                .padding(.horizontal, 12)
                .padding(.vertical, 6)
                .background(PassmateColors.weakTopicBg)
                .clipShape(Capsule())
        }
    }

    var body: some View {
        if !topics.isEmpty {
            FlowLayout(items, id: \.self, spacing: 8) { item in
                itemView(item)
            }
        }
    }
}
```

- [ ] **Step 4: `RatingSectionView.swift` 호출부 치환 + private FlowLayout 삭제**

(a) 32~36행의
```swift
            FlowLayout(spacing: 8) {
                ForEach(RatingTag.companion.all, id: \.self) { tag in
                    tagChip(tag)
                }
            }
```
을
```swift
            FlowLayout(RatingTag.companion.all, id: \.self, spacing: 8) { tag in
                tagChip(tag)
            }
```
으로 바꾼다.

(b) 134행 주석 `// 태그 칩 가로 흐름 배치 (iOS 16+) — RatingSection 전용 로컬 FlowLayout`부터 `private struct FlowLayout: Layout {` … 닫는 `}`(원본 178행) 및 그 다음 빈 줄(179행)까지 삭제한다. 삭제 후 `}`(원본 132행) → 빈 줄 → `// MARK: - 프리뷰 …` 순서가 되어야 한다.

확인: `grep -n "FlowLayout\|ProposedViewSize" iosApp/iosApp/ui/result/RatingSectionView.swift` → 호출부 1줄만.

- [ ] **Step 5: `ResultView.swift` private FlowLayout·WeakTopicsRow 삭제 (아래쪽부터)**

(a) 343행 주석 `// 칩·주제 태그를 가로로 흐르게 배치하는 간단한 FlowLayout (iOS 16+)`부터 `private struct FlowLayout: Layout {` … 닫는 `}`(390행) 및 다음 빈 줄(391행)까지 삭제. 삭제 후 `ShareSheet` 닫는 `}` → 빈 줄 → `// MARK: - 프리뷰 …` 순서.

(b) 216행 `private struct WeakTopicsRow: View {`부터 닫는 `}`(240행) 및 다음 빈 줄(241행)까지 삭제. 삭제 후 `ReportHeaderCard` 닫는 `}` → 빈 줄 → `private struct QuestionList: View {` 순서.

`ResultContentView` 안의 `WeakTopicsRow(topics: uiState.report?.weakTopics ?? [])`(110행) 호출은 그대로 둔다(공통 컴포넌트로 해석됨).

확인: `grep -n "FlowLayout\|WeakTopicsRow\|ProposedViewSize" iosApp/iosApp/ui/result/ResultView.swift` → `WeakTopicsRow(topics:` 호출 1줄만.

- [ ] **Step 6: `JoinedRoomsView.swift` private FlowLayout·WeakTopicsRow 삭제 (아래쪽부터)**

(a) 389행 주석 `// 칩 가로 흐름 배치 (iOS 16+)`부터 `private struct FlowLayout: Layout {` … 닫는 `}`(436행) 및 다음 빈 줄(437행)까지 삭제. 삭제 후 직전 `}`(387행) → 빈 줄 → `// MARK: - 프리뷰 …` 순서.

(b) 250행 `private struct WeakTopicsRow: View {`부터 닫는 `}`(274행) 및 다음 빈 줄(275행)까지 삭제. 삭제 후 `SummaryCard` 닫는 `}` → 빈 줄 → `private struct JoinedRoomRow: View {` 순서.

`WeakTopicsRow(topics: summary.weakTopics)`(103행) 호출은 그대로.

확인: `grep -n "FlowLayout\|WeakTopicsRow\|ProposedViewSize" iosApp/iosApp/ui/mypage/JoinedRoomsView.swift` → `WeakTopicsRow(topics:` 호출 1줄만.

- [ ] **Step 7: pbxproj에 idx 156·157 등록 (8줄)**

앵커 줄 바로 뒤에 추가(Task 1과 같은 앵커. 이미 158 줄이 있으면 154 앵커와 158 줄 사이에 들어가도록 154 앵커 뒤에 넣는다).

1. PBXBuildFile — 앵커 `A1010001154AABBCCDDEEFF0154 /* SettingsView.swift in Sources */ = {…};` 뒤:
```
		A1010001156AABBCCDDEEFF0156 /* FlowLayout.swift in Sources */ = {isa = PBXBuildFile; fileRef = A1011001156AABBCCDDEEFF0156 /* FlowLayout.swift */; };
		A1010001157AABBCCDDEEFF0157 /* WeakTopicsRow.swift in Sources */ = {isa = PBXBuildFile; fileRef = A1011001157AABBCCDDEEFF0157 /* WeakTopicsRow.swift */; };
```
2. PBXFileReference — 앵커 `A1011001154AABBCCDDEEFF0154 /* SettingsView.swift */ = {…};` 뒤:
```
		A1011001156AABBCCDDEEFF0156 /* FlowLayout.swift */ = {isa = PBXFileReference; lastKnownFileType = sourcecode.swift; path = FlowLayout.swift; sourceTree = "<group>"; };
		A1011001157AABBCCDDEEFF0157 /* WeakTopicsRow.swift */ = {isa = PBXFileReference; lastKnownFileType = sourcecode.swift; path = WeakTopicsRow.swift; sourceTree = "<group>"; };
```
3. `component` 그룹 children — 앵커 `A1011066AABBCCDDEEFF0066 /* PassyMascot.swift */,` 뒤:
```
				A1011001156AABBCCDDEEFF0156 /* FlowLayout.swift */,
				A1011001157AABBCCDDEEFF0157 /* WeakTopicsRow.swift */,
```
4. PBXSourcesBuildPhase — 앵커 `A1010001154AABBCCDDEEFF0154 /* SettingsView.swift in Sources */,` 뒤:
```
				A1010001156AABBCCDDEEFF0156 /* FlowLayout.swift in Sources */,
				A1010001157AABBCCDDEEFF0157 /* WeakTopicsRow.swift in Sources */,
```

검증:
```bash
plutil -lint iosApp/iosApp.xcodeproj/project.pbxproj
grep -E $'^\t\t[0-9A-F]+ /\\* .* \\*/ = \\{' iosApp/iosApp.xcodeproj/project.pbxproj | awk '{print $1}' | sort | uniq -d
grep -c "AABBCCDDEEFF0156" iosApp/iosApp.xcodeproj/project.pbxproj; grep -c "AABBCCDDEEFF0157" iosApp/iosApp.xcodeproj/project.pbxproj
```
Expected: `OK` / 빈 출력 / `4` `4`

- [ ] **Step 8: 그린 확인 — Layout 오류 0, 일반 빌드 성공**

```bash
cd /Users/pocari/workspace/projects/pass-mate/Passmate-KMP
SCRATCH=/private/tmp/claude-501/-Users-pocari-workspace-projects-pass-mate-Passmate-KMP/582e7a59-aee1-47e1-91d2-6a86fe25d44e/scratchpad
xcodebuild -project /Users/pocari/workspace/projects/pass-mate/Passmate-KMP/iosApp/iosApp.xcodeproj -scheme iosApp -configuration Debug -destination "generic/platform=iOS Simulator" -derivedDataPath /Users/pocari/workspace/projects/pass-mate/Passmate-KMP/iosApp/build/DerivedData CODE_SIGNING_ALLOWED=NO IPHONEOS_DEPLOYMENT_TARGET=15.0 'OTHER_SWIFT_FLAGS=$(inherited) -continue-building-after-errors' build > "$SCRATCH/ios15-task2.log" 2>&1
grep -E "error:" "$SCRATCH/ios15-task2.log" | sed 's#.*/iosApp/iosApp/##' | sort -u
xcodebuild -project /Users/pocari/workspace/projects/pass-mate/Passmate-KMP/iosApp/iosApp.xcodeproj -scheme iosApp -configuration Debug -destination "generic/platform=iOS Simulator" -derivedDataPath /Users/pocari/workspace/projects/pass-mate/Passmate-KMP/iosApp/build/DerivedData CODE_SIGNING_ALLOWED=NO build 2>&1 | grep -E "\*\* BUILD|error:"
git checkout -- gradlew
```
Expected: 오류 3줄(전부 `ContentView.swift`: NavigationStack · navigationDestination · toolbar), 일반 빌드 `** BUILD SUCCEEDED **`.

- [ ] **Step 9: 커밋**

```bash
git status --short
git add iosApp/iosApp/component/FlowLayout.swift iosApp/iosApp/component/WeakTopicsRow.swift iosApp/iosApp/ui/result/RatingSectionView.swift iosApp/iosApp/ui/result/ResultView.swift iosApp/iosApp/ui/mypage/JoinedRoomsView.swift iosApp/iosApp.xcodeproj/project.pbxproj
git commit -m "refactor(ios): FlowLayout을 iOS 15 호환 데이터 기반 공통 컴포넌트로 통합(Layout 프로토콜 3벌 삭제), WeakTopicsRow 공통 승격(2벌 삭제) (iOS 15 호환 스펙 §3)

Claude-Session: https://claude.ai/code/session_01KmjdULniHZh2JYamx3rkjj"
```

---

### Task 3: 탭 셸 — `RouteStackLevel` + `ContentView` 단일 `NavigationView` 스택

**Files:**
- Create: `iosApp/iosApp/navigation/RouteStackLevel.swift`
- Modify: `iosApp/iosApp/ContentView.swift:1-116` (머리말 주석·상태·`body`·`tabSelection`·`currentPath`·`tabStack`과 뒤따르는 빈 줄 — 117행 `@ViewBuilder`/`private func destinationView` 이후는 무변경)
- Modify: `iosApp/iosApp.xcodeproj/project.pbxproj` (idx 155 등록 4줄)

**Interfaces:**
- Consumes: `enum Route: Hashable`(`navigation/Route.swift`), `AppTab`, `AppShellViewModel`(무변경)
- Produces: `struct RouteStackLevel<Destination: View>: View` — `init(path: Binding<[Route]>, index: Int, @ViewBuilder destination: @escaping (Route, Binding<[Route]>) -> Destination)`

- [ ] **Step 1: 레드 확인 — 기준 로그에서 ContentView 오류 3건**

```bash
SCRATCH=/private/tmp/claude-501/-Users-pocari-workspace-projects-pass-mate-Passmate-KMP/582e7a59-aee1-47e1-91d2-6a86fe25d44e/scratchpad
grep -E "ContentView.swift.*error:" "$SCRATCH/ios15-baseline.log" | sed 's#.*/iosApp/iosApp/##' | sort -u
```
Expected: `ContentView.swift:107` NavigationStack · `:109` navigationDestination(for:destination:) · `:112` toolbar(_:for:)

- [ ] **Step 2: `RouteStackLevel.swift` 작성**

```swift
import SwiftUI

// iOS 15 호환 배열 기반 push — [Route]의 한 레벨(path[index])을 그리고, 다음 레벨로 가는 숨은 NavigationLink를 정확히 1개 가진다.
// NavigationStack(path:) 대체. iOS 15 NavigationView는 한 뷰에 링크가 여럿이면 오동작하므로 레벨을 재귀로 쌓는다 (스펙 2026-08-31 §2-3, 규칙 §2-1)
struct RouteStackLevel<Destination: View>: View {
    @Binding var path: [Route]

    let index: Int

    let destination: (Route, Binding<[Route]>) -> Destination

    // 이 레벨이 마지막으로 그린 라우트 — 경로가 줄어 밀려나가는(pop) 동안 빈 화면이 되지 않게 유지한다
    @State private var shownRoute: Route?

    private var currentRoute: Route? {
        if index < path.count {
            return path[index]
        } else {
            return shownRoute
        }
    }

    // 다음 레벨 링크 활성 여부. 시스템 pop으로 false가 되면 배열을 잘라 동기화한다
    private var isNextActive: Binding<Bool> {
        Binding(
            get: { path.count > index + 1 },
            set: { isActive in
                if !isActive && path.count > index + 1 {
                    path.removeSubrange((index + 1)...)
                }
            }
        )
    }

    private func syncShownRoute(_ newPath: [Route]) {
        if index < newPath.count {
            shownRoute = newPath[index]
        }
    }

    var body: some View {
        Group {
            if let route = currentRoute {
                destination(route, $path)
            } else {
                EmptyView()
            }
        }
        .navigationBarHidden(true)
        .background(
            NavigationLink(isActive: isNextActive) {
                RouteStackLevel(path: $path, index: index + 1, destination: destination)
            } label: {
                EmptyView()
            }
            .isDetailLink(false)
        )
        .onAppear { syncShownRoute(path) }
        .onChange(of: path) { syncShownRoute($0) }
    }

    init(
        path: Binding<[Route]>,
        index: Int,
        @ViewBuilder destination: @escaping (Route, Binding<[Route]>) -> Destination
    ) {
        self._path = path
        self.index = index
        self.destination = destination
    }
}
```

- [ ] **Step 3: `ContentView.swift` 1~116행 교체**

파일의 1행부터 116행(`tabStack` 닫는 `}`(115행)와 그 다음 빈 줄(116행)까지)을 아래 블록으로 **통째로 교체**한다(블록 끝의 빈 줄이 116행을 대신한다). 117행 `@ViewBuilder`/`private func destinationView(for:path:)`부터 파일 끝(`ContentView_Previews` 포함)은 그대로 둔다.

```swift
import SwiftUI
import Shared

// 하단 4탭 셸 — NavigationView(stack) 하나가 TabView를 감싸는 단일 [Route] 스택. push된 화면은 TabView 전체를 덮어 탭 바가 숨는다
// (규칙 §2-1, iOS 15 호환 스펙 2026-08-31 §2). 앱 시작 기본 진입은 항상 Home(게스트 포함, 규칙 §2-1-1)
struct ContentView: View {
    @StateObject private var shellViewModel = AppShellViewModel(
        isSignedInUseCase: KoinHelper.shared.isSignedInUseCase()
    )

    @State private var selectedTab: AppTab = .home

    // 탭 루트 위에 쌓인 push 경로 — 탭별 보존 없음(push 중엔 탭 바가 숨어 전환 불가, Android·Desktop과 동일)
    @State private var path: [Route] = []

    // 로그인·로그아웃·탈퇴 시 증가 — NavigationView 전체를 재생성해 탭 루트(JoinView·MyInfoView 등)가 세션 상태를 다시 읽게 한다 (규칙 §8)
    @State private var sessionGeneration = 0

    var body: some View {
        NavigationView {
            TabView(selection: tabSelection) {
                // 홈 탭 = 입장 폼 인라인 (M-01 v6) — JoinView 재사용
                JoinView(
                    initialPin: nil,
                    onJoined: { pin in path.append(.waiting(pin: pin)) },
                    onPaymentRequired: { pin in path.append(.payment(pin: pin)) },
                    onSignInRequested: { path.append(.signIn) }
                )
                .tabItem { Label(AppTab.home.label, systemImage: AppTab.home.systemImage) }
                .tag(AppTab.home)

                HostedRoomsView(
                    onRequireSignIn: { path.append(.signIn) },
                    onOpenReputation: { path.append(.reputation) },
                    onOpenRoomReport: { roomId in path.append(.roomReport(roomId: roomId)) },
                    onOpenSessionControl: { roomId, pin in path.append(.sessionControl(roomId: roomId, pin: pin)) }
                )
                .tabItem { Label(AppTab.hostedRooms.label, systemImage: AppTab.hostedRooms.systemImage) }
                .tag(AppTab.hostedRooms)

                JoinedRoomsView(
                    onRequireSignIn: { path.append(.signIn) },
                    onOpenReport: { roomId in path.append(.result(roomId: roomId)) },
                    onRejoin: { pin in path.append(.waiting(pin: pin)) }
                )
                .tabItem { Label(AppTab.joinedRooms.label, systemImage: AppTab.joinedRooms.systemImage) }
                .tag(AppTab.joinedRooms)

                MyInfoView(
                    onRequireSignIn: { path.append(.signIn) },
                    onOpenReputation: { path.append(.reputation) },
                    onOpenCoinHistory: { path.append(.coinHistory) },
                    onOpenEarnings: { path.append(.earnings) },
                    onOpenSettings: { path.append(.settings) },
                    onSignedOut: {
                        path = []
                        selectedTab = .home
                        sessionGeneration += 1
                    }
                )
                .tabItem { Label(AppTab.myInfo.label, systemImage: AppTab.myInfo.systemImage) }
                .tag(AppTab.myInfo)
            }
            .tint(PassmateColors.primary)
            .navigationBarHidden(true)
            .background(
                NavigationLink(isActive: isStackActive) {
                    RouteStackLevel(path: $path, index: 0) { route, path in
                        destinationView(for: route, path: path)
                    }
                } label: {
                    EmptyView()
                }
                .isDetailLink(false)
            )
        }
        .navigationViewStyle(.stack)
        .id(sessionGeneration)
        .onReceive(shellViewModel.event) { event in
            switch event {
            case let .navigateToTab(tab):
                selectedTab = tab
            case .requireSignIn:
                // 탭 루트 위에 로그인 push — 로그인 후 pendingRoute 복귀는 후속 작업(최상단 교체로 구현, 스펙 §2-5)
                path.append(.signIn)
            }
        }
    }

    // 탭 선택은 셸 가드를 거친다 — 게스트의 로그인 필수 탭은 SignIn으로 (결정 2)
    private var tabSelection: Binding<AppTab> {
        Binding(
            get: { selectedTab },
            set: { shellViewModel.action(.selectTab($0)) }
        )
    }

    // 루트 링크 활성 여부(경로가 비어 있지 않으면 push). 시스템 pop으로 false가 되면 경로를 비운다
    private var isStackActive: Binding<Bool> {
        Binding(
            get: { !path.isEmpty },
            set: { isActive in
                if !isActive {
                    path = []
                }
            }
        )
    }

```

교체 후 확인:
```bash
grep -n "NavigationStack\|navigationDestination\|toolbar(\|homePath\|hostedRoomsPath\|joinedRoomsPath\|myInfoPath\|currentPath\|tabStack" iosApp/iosApp/ContentView.swift
```
Expected: 빈 출력(전부 사라짐). `grep -n "private func destinationView\|private func popOnce\|struct ContentView_Previews" iosApp/iosApp/ContentView.swift`는 3줄 모두 존재.

- [ ] **Step 4: pbxproj에 idx 155 등록 (4줄)**

1. PBXBuildFile — 앵커 `A1010001154AABBCCDDEEFF0154 /* SettingsView.swift in Sources */ = {…};` 뒤:
```
		A1010001155AABBCCDDEEFF0155 /* RouteStackLevel.swift in Sources */ = {isa = PBXBuildFile; fileRef = A1011001155AABBCCDDEEFF0155 /* RouteStackLevel.swift */; };
```
2. PBXFileReference — 앵커 `A1011001154AABBCCDDEEFF0154 /* SettingsView.swift */ = {…};` 뒤:
```
		A1011001155AABBCCDDEEFF0155 /* RouteStackLevel.swift */ = {isa = PBXFileReference; lastKnownFileType = sourcecode.swift; path = RouteStackLevel.swift; sourceTree = "<group>"; };
```
3. `A1012019AABBCCDDEEFF0019 /* navigation */` 그룹 children — 앵커 `A1011001149AABBCCDDEEFF0149 /* AppShellViewModel.swift */,` 뒤:
```
				A1011001155AABBCCDDEEFF0155 /* RouteStackLevel.swift */,
```
4. PBXSourcesBuildPhase — 앵커 `A1010001154AABBCCDDEEFF0154 /* SettingsView.swift in Sources */,` 뒤:
```
				A1010001155AABBCCDDEEFF0155 /* RouteStackLevel.swift in Sources */,
```

검증:
```bash
plutil -lint iosApp/iosApp.xcodeproj/project.pbxproj
grep -E $'^\t\t[0-9A-F]+ /\\* .* \\*/ = \\{' iosApp/iosApp.xcodeproj/project.pbxproj | awk '{print $1}' | sort | uniq -d
grep -c "AABBCCDDEEFF0155" iosApp/iosApp.xcodeproj/project.pbxproj
```
Expected: `OK` / 빈 출력 / `4`

- [ ] **Step 5: 그린 확인 — iOS 15 검사 빌드 오류 0, 일반 빌드 성공**

```bash
cd /Users/pocari/workspace/projects/pass-mate/Passmate-KMP
SCRATCH=/private/tmp/claude-501/-Users-pocari-workspace-projects-pass-mate-Passmate-KMP/582e7a59-aee1-47e1-91d2-6a86fe25d44e/scratchpad
xcodebuild -project /Users/pocari/workspace/projects/pass-mate/Passmate-KMP/iosApp/iosApp.xcodeproj -scheme iosApp -configuration Debug -destination "generic/platform=iOS Simulator" -derivedDataPath /Users/pocari/workspace/projects/pass-mate/Passmate-KMP/iosApp/build/DerivedData CODE_SIGNING_ALLOWED=NO IPHONEOS_DEPLOYMENT_TARGET=15.0 'OTHER_SWIFT_FLAGS=$(inherited) -continue-building-after-errors' build > "$SCRATCH/ios15-task3.log" 2>&1
grep -E "\*\* BUILD|error:" "$SCRATCH/ios15-task3.log" | sed 's#.*/iosApp/iosApp/##' | sort -u
grep -c "only available in iOS 16" "$SCRATCH/ios15-task3.log"
xcodebuild -project /Users/pocari/workspace/projects/pass-mate/Passmate-KMP/iosApp/iosApp.xcodeproj -scheme iosApp -configuration Debug -destination "generic/platform=iOS Simulator" -derivedDataPath /Users/pocari/workspace/projects/pass-mate/Passmate-KMP/iosApp/build/DerivedData CODE_SIGNING_ALLOWED=NO build 2>&1 | grep -E "\*\* BUILD|error:"
git checkout -- gradlew
```
Expected: 검사 빌드 `** BUILD SUCCEEDED **`, 오류 0, "only available in iOS 16" 경고·오류 0. 일반 빌드 `** BUILD SUCCEEDED **`.

- [ ] **Step 6: 커밋**

```bash
git status --short
git add iosApp/iosApp/navigation/RouteStackLevel.swift iosApp/iosApp/ContentView.swift iosApp/iosApp.xcodeproj/project.pbxproj
git commit -m "feat(ios): 탭 셸을 NavigationView 단일 [Route] 스택으로 교체 — 재귀 RouteStackLevel(레벨당 숨은 NavigationLink 1개)로 NavigationStack(path:) 대체, push 시 TabView 전체가 밀려 탭 바 숨김 (iOS 15 호환 스펙 §2)

Claude-Session: https://claude.ai/code/session_01KmjdULniHZh2JYamx3rkjj"
```

---

### Task 4: 배포 타깃 15.0 — pbxproj 설정값 변경 + 전체 빌드 그린

**Files:**
- Modify: `iosApp/iosApp.xcodeproj/project.pbxproj` — `IPHONEOS_DEPLOYMENT_TARGET = 16.0;` 2곳(Debug·Release 프로젝트 빌드 설정, 현재 856·919행 부근)

- [ ] **Step 1: 레드 확인 — 현재 설정값이 16.0**

```bash
grep -n "IPHONEOS_DEPLOYMENT_TARGET" iosApp/iosApp.xcodeproj/project.pbxproj
```
Expected: 2줄, 모두 `= 16.0;`

- [ ] **Step 2: 2곳을 15.0으로 변경**

두 줄의 `IPHONEOS_DEPLOYMENT_TARGET = 16.0;` → `IPHONEOS_DEPLOYMENT_TARGET = 15.0;`

```bash
sed -i '' 's/IPHONEOS_DEPLOYMENT_TARGET = 16.0;/IPHONEOS_DEPLOYMENT_TARGET = 15.0;/g' iosApp/iosApp.xcodeproj/project.pbxproj
grep -n "IPHONEOS_DEPLOYMENT_TARGET" iosApp/iosApp.xcodeproj/project.pbxproj
plutil -lint iosApp/iosApp.xcodeproj/project.pbxproj
```
Expected: 2줄 모두 `= 15.0;`, `OK`

- [ ] **Step 3: 그린 확인 — 설정값 그대로(오버라이드 없이) 일반 빌드 성공 + 16+ 가용성 경고 0**

```bash
cd /Users/pocari/workspace/projects/pass-mate/Passmate-KMP
SCRATCH=/private/tmp/claude-501/-Users-pocari-workspace-projects-pass-mate-Passmate-KMP/582e7a59-aee1-47e1-91d2-6a86fe25d44e/scratchpad
xcodebuild -project /Users/pocari/workspace/projects/pass-mate/Passmate-KMP/iosApp/iosApp.xcodeproj -scheme iosApp -configuration Debug -destination "generic/platform=iOS Simulator" -derivedDataPath /Users/pocari/workspace/projects/pass-mate/Passmate-KMP/iosApp/build/DerivedData CODE_SIGNING_ALLOWED=NO build > "$SCRATCH/ios15-task4.log" 2>&1
grep -E "\*\* BUILD|error:" "$SCRATCH/ios15-task4.log"
grep -c "only available in iOS 16" "$SCRATCH/ios15-task4.log"
grep -E "warning:.*(deprecated|only available)" "$SCRATCH/ios15-task4.log" | sed 's#.*/iosApp/iosApp/##' | sort -u | head
git checkout -- gradlew
```
Expected: `** BUILD SUCCEEDED **`, 오류 0, "only available in iOS 16" 0. 배포 타깃 15.0 기준이라 `NavigationLink(isActive:)`·`onChange(of:perform:)`는 deprecated 경고가 나지 않아야 한다(나오면 배포 타깃이 반영되지 않은 것 — Step 2 재확인).

- [ ] **Step 4: 최종 pbxproj 정합성 검사 (신규 4파일 각 4회 등록·중복 ID 0)**

```bash
for i in 155 156 157 158; do printf "%s " $i; grep -c "AABBCCDDEEFF0$i" iosApp/iosApp.xcodeproj/project.pbxproj; done
grep -E $'^\t\t[0-9A-F]+ /\\* .* \\*/ = \\{' iosApp/iosApp.xcodeproj/project.pbxproj | awk '{print $1}' | sort | uniq -d
grep -c "A10120" iosApp/iosApp.xcodeproj/project.pbxproj
```
Expected: `155 4` `156 4` `157 4` `158 4` / 빈 출력 / 그룹 ID 등장 수가 Task 1 시작 전과 동일(그룹 신설 없음 — 기준값은 `git show feature/home:iosApp/iosApp.xcodeproj/project.pbxproj | grep -c "A10120"`로 얻는다)

- [ ] **Step 5: 커밋**

```bash
git status --short
git add iosApp/iosApp.xcodeproj/project.pbxproj
git commit -m "build(ios): 최소 배포 타깃 16.0 → 15.0 복귀 — 팀 실기기(iOS 15) 호환, iOS 16+ API는 전부 공통 컴포넌트 #available 분기로 이관 완료

Claude-Session: https://claude.ai/code/session_01KmjdULniHZh2JYamx3rkjj"
```

---

### Task 5: 문서·규칙 개정

**Files:**
- Modify: `docs/Passmate_코드_패턴_규칙.md:26`(§2-1 iOS 문구), `:277` 뒤(§13 금지 1줄)
- Modify: `docs/superpowers/specs/2026-08-30-home-shell-tabs-design.md:51`(§1-4 iOS 행), `:123`(§5 첫 bullet)
- Modify: `docs/Passmate_Mac_검증_체크리스트.md` — 파일 끝에 §10 추가

- [ ] **Step 1: 규칙 §2-1 — iOS 네비게이션 문구 교체 + 배포 타깃 규칙 추가**

26행
```
- iOS는 `NavigationStack`(`NavigationPath`)을 사용한다.
```
을 아래 2줄로 교체:
```
- iOS는 `NavigationView`(stack 스타일) 위에 상태 배열 `[Route]` 기반 push(`RouteStackLevel`)를 사용한다.
- iOS 최소 배포 타깃은 15.0이다. iOS 16+ 전용 API(`NavigationStack`·`presentationDetents`·`Layout` 등)는 화면에서 직접 쓰지 않고 `#available` 분기를 가진 공통 컴포넌트(`component/`) 뒤로만 사용한다.
```

- [ ] **Step 2: 규칙 §13 — 금지 항목 추가**

`- 계약 문서에 없는 필드·이벤트를 임의 추가하는 구현 금지 (계약 갱신이 먼저)` 줄 **바로 뒤**에 추가:
```
- iOS 16+ 전용 API를 `#available` 없이 화면에서 직접 사용하는 구현 금지 (최소 배포 타깃 15.0 — 공통 컴포넌트 안에서만 분기)
```

확인: `grep -n "NavigationStack" docs/Passmate_코드_패턴_규칙.md` → §2-1의 "직접 쓰지 않고" 문장과 §13 금지 문장에서만 등장(사용 지시로는 0건).

- [ ] **Step 3: 홈 셸 스펙 §1-4 iOS 행·§5 첫 bullet 정정**

(a) 51행(`| iOS | \`TabView(selection:)\` 4개, 탭마다 \`NavigationStack(path)\`. …`로 시작하는 표 행 전체)을 아래로 교체:
```
| iOS | `NavigationView`(stack) 하나가 `TabView(selection:)`를 감싸는 **단일 `[Route]` 스택** — 배열 push는 재귀 `RouteStackLevel`이 담당하고, push 시 TabView 전체가 밀려 탭 바가 숨는다. (2026-08-31 iOS 15 호환 작업으로 "탭마다 `NavigationStack`"에서 변경 — [2026-08-31-ios15-compat-design.md](2026-08-31-ios15-compat-design.md) §2.) 로그인 필수 탭 선택은 `AppShellViewModel` 판정 후 `selection` 변경 또는 `.signIn` push. 로그인·로그아웃·탈퇴 시 `sessionGeneration`을 올려 `NavigationView`를 `.id()`로 재생성한다(탭 루트 VM이 세션을 다시 읽음) |
```

(b) 123행
```
- `ContentView`: `TabView` + 탭별 `NavigationStack`. `destinationView(for:)`를 공유 함수로 추출. 탭 선택은 `AppShellViewModel.swift`(`KoinHelper.shared.isSignedInUseCase()` 재사용) 판정을 거친다.
```
을
```
- `ContentView`: `NavigationView`(stack) + `TabView` 단일 스택(2026-08-31 iOS 15 호환 스펙 §2에서 "탭별 `NavigationStack`"을 대체). `destinationView(for:)`를 공유 함수로 추출. 탭 선택은 `AppShellViewModel.swift`(`KoinHelper.shared.isSignedInUseCase()` 재사용) 판정을 거친다.
```
으로 교체.

- [ ] **Step 4: Mac 체크리스트 §10 추가 (파일 끝에)**

```markdown

## 10. iOS 15 호환 — 배포 타깃 15.0 복귀 (fix/ios15-deployment-target, 2026-08-31 — 파트2)

> 신규 Swift 4개 pbxproj idx **155~158**(navigation `RouteStackLevel` 155 · component `FlowLayout` 156 · `WeakTopicsRow` 157 · `SheetDetents` 158). **다음 가용 idx = 159.** 그룹 ID 신규 없음. 스펙: `docs/superpowers/specs/2026-08-31-ios15-compat-design.md`
> 이 Mac엔 iOS 26.3 시뮬 런타임만 있어 **iOS 15 경로(NavigationView 재귀 스택·UIKit 시트 브리지·FlowLayout)의 정본 검증은 실기기**. 시뮬(iOS 26)은 iOS 16+ 경로(네이티브 detents)만 탄다.

- [ ] 컴파일: pbxproj `IPHONEOS_DEPLOYMENT_TARGET = 15.0` 2곳 · `xcodebuild … build` 오류 0 · "only available in iOS 16" 0 · `grep -rn "NavigationStack\|presentationDetents\|: Layout" iosApp/iosApp --include='*.swift'`는 `component/SheetDetents.swift`의 `#available` 분기 1건만
- [ ] 시뮬(iOS 26): 홈 폼 + 탭 4개 → 게스트 마이 탭 → SignIn push(탭 바 숨김) → 닫기 → 홈 탭·폼 유지
- [ ] 시뮬(iOS 26): 마이 → 설정 2단 push → 뒤로 2회 → 마이 루트 · 홈 탭 재선택 시 폼 리셋
- [ ] 시뮬(iOS 26): 방 목록 프로필 시트·마이 시트 4종 반높이(네이티브 detents 경로)
- [ ] **실기기(iOS 15)** — Xcode에서 본인 Apple ID 팀으로 서명(`Configuration/Config.xcconfig` `TEAM_ID` 설정) 후 설치:
  - [ ] 각 탭 루트 → push(SignIn·설정·명성·코인 내역·정산) → 뒤로 콜백으로 pop — push 중 탭 바 숨김, pop 후 탭 바 복귀
  - [ ] 시트 반높이: 방 목록 프로필·정산 계좌·마이 시트 4종이 반높이로 열리고 위로 끌면 전체 높이(UIKit 브리지 경로)
  - [ ] 칩 줄바꿈: 결과 화면 "보완할 주제" 행·평가 시트 태그 칩이 폭에 맞춰 줄바꿈되고 아래 요소와 겹치지 않음(FlowLayout 높이 고정)
  - [ ] `[백엔드]` Play → Result 전환: Play가 오른쪽으로 밀려나며 Result가 드러남(빈 화면·깜빡임 없음), Result "홈으로" → 탭 루트
  - [ ] 로그아웃/로그인 후 홈 탭 폼이 세션 상태를 다시 읽음(`sessionGeneration` 재생성)
  - [ ] 숨은 `NavigationLink`가 트리거되지 않는 경우(push가 전혀 안 됨) → 스펙 §2-5의 `ZStack` + `.hidden()` 대안으로 전환
```

- [ ] **Step 5: 커밋**

```bash
git add docs/Passmate_코드_패턴_규칙.md docs/superpowers/specs/2026-08-30-home-shell-tabs-design.md docs/Passmate_Mac_검증_체크리스트.md
git commit -m "docs: 규칙 §2-1 iOS 네비게이션을 NavigationView+[Route] 스택으로 개정·배포 타깃 15.0 규칙 추가, §13 iOS 16+ API 직접 사용 금지, 홈 셸 스펙 §1-4/§5 iOS 행 정정, Mac 체크리스트 §10(iOS 15 호환, idx 155~158·다음 159)

Claude-Session: https://claude.ai/code/session_01KmjdULniHZh2JYamx3rkjj"
```

---

### Task 6: 검증 — 시뮬레이터 설치·실행·스크린샷 + Gradle 회귀

**Files:** 없음(검증만). 산출물은 `$SCRATCH/ios15-smoke/`.

- [ ] **Step 1: 시뮬레이터용 빌드·설치·실행**

```bash
SCRATCH=/private/tmp/claude-501/-Users-pocari-workspace-projects-pass-mate-Passmate-KMP/582e7a59-aee1-47e1-91d2-6a86fe25d44e/scratchpad
SIM=4EA9F2CB-461B-4B4A-9977-1DC38372DD99   # iPhone 17 Pro, iOS 26.3 (다르면 `xcrun simctl list devices available | grep iPhone`로 확인)
mkdir -p "$SCRATCH/ios15-smoke"
xcrun simctl boot $SIM 2>/dev/null || true
xcodebuild -project /Users/pocari/workspace/projects/pass-mate/Passmate-KMP/iosApp/iosApp.xcodeproj -scheme iosApp -configuration Debug -destination "platform=iOS Simulator,id=$SIM" -derivedDataPath /Users/pocari/workspace/projects/pass-mate/Passmate-KMP/iosApp/build/DerivedData CODE_SIGNING_ALLOWED=NO build 2>&1 | grep -E "\*\* BUILD|error:"
APP=$(find /Users/pocari/workspace/projects/pass-mate/Passmate-KMP/iosApp/build/DerivedData/Build/Products/Debug-iphonesimulator -maxdepth 1 -name "*.app" | head -1); echo "$APP"
xcrun simctl install $SIM "$APP"
xcrun simctl launch $SIM org.sesacteamproject.passmate.Passmate
sleep 4
xcrun simctl io $SIM screenshot "$SCRATCH/ios15-smoke/01-home-tabs.png"
xcrun simctl spawn $SIM launchctl list | grep -i passmate
git checkout -- gradlew
```
Expected: `** BUILD SUCCEEDED **`, 앱 경로 출력, 런치 후 프로세스가 살아 있음(크래시 없음), 스크린샷에 입장 폼 + 하단 탭 4개(홈·내가 만든 방·참여한 방·마이).

- [ ] **Step 2: 스크린샷 확인**

`Read` 도구로 `$SCRATCH/ios15-smoke/01-home-tabs.png`를 열어 (1) 상단에 시스템 내비바가 보이지 않는지(`navigationBarHidden`), (2) 탭 바 4개가 tint Primary로 보이는지, (3) 홈 폼이 이전(feature/home 스크린샷)과 동일한 레이아웃인지 확인한다. 상단에 빈 내비바 띠가 보이면 `ContentView`의 `TabView`에 `.navigationBarHidden(true)`가 빠진 것이다.

- [ ] **Step 3: 탭·push 스모크 (사용자 수동 — Simulator 창에서 진행, 체크리스트 §10 시뮬 항목)**

시뮬레이터 탭은 터미널에서 자동화하지 않는다. 사용자가 Simulator 창에서 §10의 "시뮬(iOS 26)" 3항목을 확인하고 결과를 알려주면 기록한다. 실행자는 그동안 Step 4를 진행한다.

- [ ] **Step 4: Gradle 회귀(Kotlin 무변경 확인)**

```bash
cd /Users/pocari/workspace/projects/pass-mate/Passmate-KMP
git diff --stat feature/home -- shared composeApp | tail -1
sh gradlew :composeApp:jvmTest :shared:compileDebugKotlinAndroid :composeApp:compileDebugKotlinAndroid 2>&1 | tail -5
```
Expected: `git diff --stat`는 빈 출력(Kotlin 변경 0), Gradle `BUILD SUCCESSFUL`(jvmTest 21/21은 이전과 동일).

- [ ] **Step 5: 결과 기록 (커밋 없음)**

`$SCRATCH/ios15-smoke/RESULT.md`에 Step 1·2·4 결과와 Step 3의 사용자 확인 결과를 적는다. PR 본문(Task 7)에서 인용한다.

---

### Task 7: PR 본문·PR #16 코멘트/본문 초안 (게시는 사용자)

**Files:**
- Create: `$SCRATCH/pr-ios15-body.md`, `$SCRATCH/pr16-comment.md`, `$SCRATCH/pr16-body.md`

- [ ] **Step 1: 이 브랜치 PR 본문 초안**

`$SCRATCH/pr-ios15-body.md`:
```markdown
## 📝 작업 내용

팀 실기기(iOS 15)에서 동작하도록 **최소 배포 타깃을 15.0으로 되돌리고**, iOS 16+ 전용 API를 iOS 15 호환 구현으로 교체했습니다. 스펙: `docs/superpowers/specs/2026-08-31-ios15-compat-design.md`

**배경**: 템플릿 원본이 15.0이었고 PR #16이 Mac 컴파일을 위해 16.0으로 올렸습니다. 그런데 develop의 `ContentView`도 이미 `NavigationStack`(iOS 16+)을 쓰고 있어 15.0은 검증된 값이 아니었습니다 — 이 PR이 iOS 15 호환의 최초 구현입니다. 15.0 기준 빌드 오류 20건/8파일을 전부 해소했습니다.

**변경**
- 탭 셸: `NavigationView`(stack) 하나가 `TabView`를 감싸는 단일 `[Route]` 스택. 배열 push는 재귀 `RouteStackLevel`(레벨당 숨은 `NavigationLink` 1개). push 시 TabView 전체가 밀려 탭 바 숨김. **화면 파일은 콜백 배선 그대로**(`destinationView` 무변경). 탭별 스택 보존은 없음 — push 중엔 탭 바가 숨어 전환이 불가능해 체감 차이 없고, Android·Desktop과 동일.
- `FlowLayout`(Layout 프로토콜, 3벌 중복) → iOS 15 호환 데이터 기반 공통 1벌. `WeakTopicsRow`(2벌 중복) 공통 승격.
- 시트 반높이: `passmateDetents()` — iOS 16+ 네이티브 / iOS 15 UIKit `UISheetPresentationController` 브리지. 호출부 5곳.
- pbxproj: 배포 타깃 15.0, 신규 4파일 idx 155~158(다음 159).

**규칙 개정 — 리뷰 요청 (홍희표 님)**
- §2-1: "iOS는 `NavigationStack`(`NavigationPath`)" → "`NavigationView`(stack) 위에 `[Route]` 기반 push(`RouteStackLevel`)" + "최소 배포 타깃 15.0, iOS 16+ API는 공통 컴포넌트 안 `#available` 분기로만".
- §13: iOS 16+ API를 `#available` 없이 화면에서 직접 사용 금지.
- 파트1 iOS 화면은 이미 콜백 방식이라 코드 변경 없음. 앞으로 새 화면도 `NavigationStack`·`presentationDetents`·`Layout`을 직접 쓰지 않으면 됩니다.

**검증**
- 15.0 설정으로 `xcodebuild` 성공, "only available in iOS 16" 0.
- iOS 26.3 시뮬: 홈 폼+탭 4개 실행 확인(스크린샷 첨부). Kotlin 무변경 — `:composeApp:jvmTest` 21/21.
- **iOS 15 실기기 검증은 이 Mac에 런타임이 없어 미실시** — 체크리스트 §10 실기기 항목으로 확인 부탁드립니다(특히 시트 반높이·칩 줄바꿈·Play→Result 전환).

**선행 PR**: #16, #17, #18 위에 스택되어 있습니다. #18 병합 후 `git rebase develop`으로 이 변경만 남깁니다.

https://claude.ai/code/session_01KmjdULniHZh2JYamx3rkjj
```

- [ ] **Step 2: PR #16 코멘트 초안**

`$SCRATCH/pr16-comment.md`:
```markdown
배포 타깃 15→16은 Xcode 26에서 컴파일하기 위한 임시 조치였습니다. develop의 `ContentView`가 이미 `NavigationStack`(iOS 16+)을 쓰고 있어 15.0으로는 원래 빌드가 안 됐습니다.

실기기가 iOS 15라는 요청에 따라 후속 PR(`fix/ios15-deployment-target`, #18 위에 스택)에서 **15.0으로 되돌리고 iOS 16+ API를 전부 iOS 15 호환 구현으로 교체**했습니다. 이 PR(#16)은 그대로 병합해 주시면 후속 PR이 타깃을 15.0으로 정리합니다.
```

- [ ] **Step 3: PR #16 올바른 본문 초안 (현재 #17 본문이 잘못 들어가 있음)**

`$SCRATCH/pr16-body.md` — `git show fix/ios-build --stat` 및 커밋 메시지(`1707359`·`a5322f6`)를 근거로 작성:
```markdown
## 📝 작업 내용

develop의 iOS 코드를 Xcode 26 시뮬레이터에서 처음 빌드하며 드러난 문제를 고쳤습니다. 뷰 로직 변경은 없습니다.

- **시뮬레이터 arm64 허용**: 템플릿의 `EXCLUDED_ARCHS[sdk=iphonesimulator*] = arm64` 제거(이게 있으면 Apple Silicon Mac에서 기기 목적지가 잡히지 않음).
- **Kotlin 인터롭 이름 정정**: 중첩 클래스는 `AppError.Gone` 형태, 제네릭은 `PagedResult<AnyObject>`, companion은 `RatingTag.companion.all`. Swift 로컬 `enum HostLevel`이 Kotlin 타입을 가리는 문제는 `.level`로 접근.
- **프리뷰 샘플 데이터**: T116 계약(2026-08-29)에서 늘어난 `RoomHost.userId`·`PublicRoom.hostId` 인자 반영.
- **배포 타깃 15.0 → 16.0(임시)**: `NavigationStack`·`presentationDetents`·`Layout` 등 iOS 16+ API 사용으로 15.0에선 컴파일 불가. → 실기기(iOS 15) 요청에 따라 후속 PR `fix/ios15-deployment-target`에서 15.0으로 되돌리고 API를 교체합니다.

**검증**: Xcode 26.3 / iPhone 17 Pro(iOS 26.3) 시뮬레이터 빌드·실행 성공.

https://claude.ai/code/session_01KmjdULniHZh2JYamx3rkjj
```

- [ ] **Step 4: 사용자에게 전달**

세 초안의 경로를 알리고, push·PR 생성·#16 본문 교체·코멘트 게시는 사용자 승인/실행 사항임을 명시한다. 커밋 없음.
