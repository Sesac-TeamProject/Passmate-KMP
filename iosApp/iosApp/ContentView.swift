import SwiftUI

// 임시 진입 — NavigationStack 셸·라우트 가드는 T039(iosApp 입장·대기실 미러)에서 구성한다
struct ContentView: View {
    var body: some View {
        SignInView()
    }
}

struct ContentView_Previews: PreviewProvider {
    static var previews: some View {
        ContentView()
    }
}
